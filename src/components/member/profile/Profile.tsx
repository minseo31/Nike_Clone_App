import { profileContainerStyle } from "../../../style/member/profile";

const Profile: React.FC = () => {
  return <section style={profileContainerStyle}></section>;
};

export default Profile;
